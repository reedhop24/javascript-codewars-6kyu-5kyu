// There is a queue for the self-checkout tills at the supermarket. Your task is write a function to calculate the total time required 
// for all the customers to check out!

// Input:
// customers: an array of positive integers representing the queue. Each integer represents a customer, and its value is the amount of 
// time they require to check out.

// n: a positive integer, the number of checkout tills.

// Output:
// The function should return an integer, the total time required.

function queueTime(customers, n) {
  var empty = [];
  for(var i = 0; i < n; i++) {
    empty.push(0);
  }
  var count = 0;
  for(var j = 0; j < customers.length; j++) {
    empty.sort((a, b) => {return a - b});
    empty[0] += customers[j];
    count++;
    if(count === empty.length) {
      count = 0;
    }
  }
  empty.sort((a,b) => {return b-a});
  return empty[0];
}
